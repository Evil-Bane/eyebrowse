"""Thin MCP adapter over the EyeBrowse façade (FastMCP, stateful stdio).

Import ``eyebrowse.mcp.server`` for the server instance / ``main`` entry point.
Kept side-effect-light so importing the package doesn't build the server.
"""
